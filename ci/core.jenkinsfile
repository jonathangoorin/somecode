pipelineJob(values.mainJobName) {

    def values = readJSON file: "ci/values.json"

    definition {
        cps {
            script(readFileFromWorkspace(values.mainJobScriptPath))
            sandbox()
        }
    }
}